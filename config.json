{
  "log": {
    "loglevel": "warning",
    "access": ""
  },
  "inbounds": [{
    "port": "{{ PROXY_PORT: number }}",
    "protocol": "vmess",
    "settings": {
      "clients": [{
        "id": "{{ UUID: string }}",
        "alterId": 64
      }]
    },
    "streamSettings": {
      "network": "ws",
      "wsSettings": {
        "path": "/webs"
      }
    }
  }],
  "outbounds": [{
    "protocol": "freedom",
    "settings": {}
  }]
}
